const api = require('./api');
const apinews = require('./api/news');
const apiaqueryum = require('./api/aqueryum');

module.exports = {
    api: api,
    apinews: apinews,
    apiaqueryum: apiaqueryum
};